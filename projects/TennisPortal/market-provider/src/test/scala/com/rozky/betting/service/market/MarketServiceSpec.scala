package com.rozky.betting.service.market

import org.scalatest.Spec


class MarketServiceSpec extends Spec {

  describe("createMarket") {
    it("should not create market with duplicate market id") (pending)
    it("should not create market with duplicate market id xxx") (pending)
    it("should create a market and return its id")(pendingUntilFixed())
  }
}